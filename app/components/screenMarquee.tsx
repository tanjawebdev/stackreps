import styles from '../styles/ScreenMarquee.module.scss';

export default function ScreenMarquee() {
  return (
    <section className={styles.screenMarquee + " container-fluid"}>
      Animation Screen Marquee
    </section>
  )
}
